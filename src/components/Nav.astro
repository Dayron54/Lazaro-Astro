<nav class="navbar">
  <!-- IZQUIERDA -->
  <div class="nav-left">
    <a href="/">
      <img src="/public/Logo_lazaro_negro.png" alt="Lázaro Software" />
    </a>
  </div>

  <!-- CENTRO (links escritorio) -->
  <div class="nav-center">
    <a href="/">Inicio</a>
    <a href="#">Módulos</a>
    <a href="#">Clientes</a>
    <a href="#">Nosotros</a>
  </div>

  <!-- DERECHA (acciones escritorio) -->
  <div class="nav-right">
    <a class="wompi-btn" href="https://checkout.wompi.co/" target="_blank">
      Paga con Wompi
    </a>
    <a href="https://www.facebook.com/p/Lazaro-Software-100063740972972/?locale=es_LA" target="_blank">
      <img src="/logo-facebook.png" alt="Facebook" />
    </a>
    <a href="https://www.instagram.com/lazarosoftware_/?hl=es" target="_blank">
      <img src="/logo-instagram.png" alt="Instagram" />
    </a>
  </div>

  <!-- BOTÓN MÓVIL -->
  <button class="nav-toggle" aria-label="Abrir menú">
    <span></span>
    <span></span>
    <span></span>
  </button>

  <!-- MENÚ MÓVIL -->
  <div class="nav-mobile">
    <a href="/">Inicio</a>
    <a href="#">Módulos</a>
    <a href="#">Clientes</a>
    <a href="#">Nosotros</a>
    <a href="#">Contacto</a>

    <div class="nav-mobile-actions">
      <a class="wompi-btn" href="https://checkout.wompi.co/" target="_blank">
        Paga con Wompi
      </a>
      <div class="nav-mobile-social">
        <a href="#"><img src="/logo-facebook.png" /></a>
        <a href="#"><img src="/logo-instagram.png" /></a>
      </div>
    </div>
  </div>
</nav>

<script is:inline>
  const toggle = document.querySelector('.nav-toggle');
  const mobileMenu = document.querySelector('.nav-mobile');

  toggle.addEventListener('click', () => {
    toggle.classList.toggle('active');
    mobileMenu.classList.toggle('active');
  });
</script>

<style>
  /* ===== NAV BASE ===== */
.navbar {
  position: relative;
  z-index: 10000;
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  height: 80px;
  padding: 0 3rem;
  background: #f2f2f2; /* gris claro como en la imagen */
  top: 0;
  position: sticky;
}

.nav-left img {
  height: 40px;
  padding-left: 80%;
}

/* LINKS ESCRITORIO */
.nav-center {
  display: flex;
  justify-content: right;
  padding-right: 3%;
  gap: 2rem;
}

.nav-center a {
  color: #222;
  text-decoration: none;
  font-weight: 500;
  font-size: 1rem;
  transition: color 0.2s ease;
}

.nav-center a:hover {
  color: #0b4a8f;
}

/* DERECHA ESCRITORIO */
.nav-right {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.nav-right img {
  height: 22px;
}

/* ===== BOTÓN WOMPI ===== */
.wompi-btn {
  background: #0b4a8f;
  color: #fff;
  padding: 0.7rem 1.6rem;
  border-radius: 30px;
  font-weight: 600;
  font-size: 0.95rem;
  text-decoration: none;
  white-space: nowrap;
  transition: all 0.3s ease;
  box-shadow: none;
}

.wompi-btn:hover {
  background: #083b72;
  transform: translateY(-1px);
}

/* ===== ICONOS REDES ===== */
.nav-right img,
.nav-mobile-social img {
  height: 30px;
  width: auto;
  transition: transform 0.2s;
}

.nav-right img:hover,
.nav-mobile-social img:hover {
  transform: scale(1.1);
}


/* BOTÓN HAMBURGUESA */
.nav-toggle {
  display: none;
  width: 30px;
  height: 22px;
  background: none;
  border: none;
  cursor: pointer;
  flex-direction: column;
  justify-content: space-between;
}

.nav-toggle span {
  height: 3px;
  background: white;
  border-radius: 3px;
  transition: 0.4s;
}

/* ===== MENÚ MÓVIL ===== */
.nav-mobile {
  display: none;
}

/* 📱 MÓVIL */
@media (max-width: 768px) {
  .nav-center,
  .nav-right {
    display: none;
  }

 .nav-toggle {
  display: flex;
  justify-self: end;
  grid-column: 3;   /*  fuerza la columna derecha */
  z-index: 10001;   /* asegura visibilidad */
}


  .nav-mobile {
  grid-column: 1 / -1;  /*  ocupa todo el nav */
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1.4rem;
  padding: 0;

  max-height: 0;
  overflow: hidden;
  opacity: 0;
  pointer-events: none;

  background: rgba(4, 82, 156, 0.95);
  backdrop-filter: blur(8px);

  transition: max-height 0.4s ease, opacity 0.3s ease;
}



.nav-mobile.active {
  max-height: 400px;
  padding: 1.5rem 0;
  opacity: 1;
  pointer-events: auto;
}


  .nav-mobile a {
    color: white;
    text-decoration: none;
    font-size: 1.1rem;
  }

  /* Acciones móviles */
  .nav-mobile-actions {
    margin-top: 1rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }

  .nav-mobile-social {
    display: flex;
    gap: 1rem;
  }

  .nav-mobile-social img {
    height: 22px;
  }

  /* ❌ animación botón */
  .nav-toggle.active span:nth-child(1) {
    transform: rotate(45deg) translate(6px, 6px);
  }

  .nav-toggle.active span:nth-child(2) {
    opacity: 0;
  }

  .nav-toggle.active span:nth-child(3) {
    transform: rotate(-45deg) translate(6px, -6px);
  }
}

</style>

