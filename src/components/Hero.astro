<section class="hero">
  <!-- SLIDES -->
  <div class="hero__slides">
    <div class="hero__slide active">
      <img src="/public/img-content1.jpg" alt="Hero 1" />
    </div>
    <div class="hero__slide">
      <img src="/public/img-content2.jpg" alt="Hero 2" />
    </div>
    <div class="hero__slide">
      <img src="/public/img-content1.jpg" alt="Hero 3" />
    </div>
  </div>

  <!-- GRADIENTE -->
  <div class="hero__overlay"></div>

  <!-- TEXTO -->
  <div class="hero__content">
    <h2>
      Somos un sistema administrativo y contable completo,
      <span>con las últimas actualizaciones de Facturación Electrónica</span>
    </h2>

    <h1>
      Administra <br />
      tus ventas <br />
      e inventarios
    </h1>

    <a href="#contacto" class="hero__btn">¡Cotiza ahora!</a>
  </div>

  <!-- JS SLIDER -->
  <script>
    let current = 0;
    const slides = document.querySelectorAll('.hero__slide');

    setInterval(() => {
      slides[current].classList.remove('active');
      current = (current + 1) % slides.length;
      slides[current].classList.add('active');
    }, 5000);
  </script>
</section>


<style>
/* ===== HERO BASE ===== */
.hero {
  position: relative;
  width: 100%;
  height: 520px;
  overflow: hidden;
  color: #fff;
}

/* SLIDES */
.hero__slides {
  position: absolute;
  inset: 0;
}

.hero__slide {
  position: absolute;
  inset: 0;
  opacity: 0;
  transition: opacity 1s ease;
}

.hero__slide.active {
  opacity: 1;
}

.hero__slide img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* GRADIENTE (desktop) */
.hero__overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to right,
    rgb(0, 50, 150) 0%,
    rgb(0, 50, 150) 45%,
    rgba(0, 50, 150, 0) 100%
  );
  z-index: 1;
}

/* CONTENIDO */
.hero__content {
  position: relative;
  z-index: 2;
  max-width: 700px;
  padding: 3rem;
}

.hero__content h1 {
  font-size: clamp(2rem, 4vw, 3rem);
  font-weight: 800;
  line-height: 1.1;
}

.hero__content h2 {
  font-size: clamp(1rem, 1.5vw, 1.3rem);
  font-weight: 400;
  margin-bottom: 1rem;
}

.hero__content h2 span {
  font-weight: 700;
}

.hero__btn {
  display: inline-block;
  margin-top: 1rem;
  padding: 0.8rem 1.6rem;
  background: #00aaff;
  color: #fff;
  font-size: 1.2rem;
  font-weight: bold;
  border-radius: 25px;
  text-decoration: none;
}

/* ===== 📱 MÓVIL ===== */
@media (max-width: 768px) {
  .hero {
    height: auto;
    min-height: 480px;
  }

  .hero__overlay {
    background: linear-gradient(
      to bottom,
      rgba(0, 50, 150, 0.95) 0%,
      rgba(0, 50, 150, 0.85) 60%,
      rgba(0, 50, 150, 0.95) 100%
    );
  }

  .hero__content {
    max-width: 100%;
    padding: 2rem 1.5rem;
    text-align: center;
  }

  .hero__btn {
    font-size: 1.1rem;
  }
}


</style>
