<section class="hero">
  <div class="hero-container">
    <!-- IZQUIERDA -->
    <div class="hero-content">
      <span class="hero-badge">
        <span class="dot"></span>
        CONEXIÓN DIRECTA CON LA DIAN
      </span>

      <h1>
        Administra tus
        <span class="blue">ventas e</span>
        <span class="blue">inventarios</span>
      </h1>

      <p>
        Somos el sistema administrativo y contable más robusto, con las últimas
        actualizaciones de Facturación Electrónica para que tu negocio nunca se
        detenga.
      </p>

      <div class="hero-buttons">
        <a
          href="https://wa.me/573173718032?text=Buen%20d%C3%ADa.%0A%0AMi%20nombre%20es%3A%0A%0AY%20requiero%20asesor%C3%ADa%20de%20ventas."
          class="btn-primary" target="_blank">¡Cotiza ahora!</a
        >
        <a href="#" target="_blank" class="btn-secondary" > ▶ Ver Demo </a>
      </div>
    </div>

    <!-- DERECHA -->
    <div class="hero-image">
  <div class="image-card">
    <img id="hero-slider" src="/img-content1.jpg" alt="Sistema Lázaro" />
  </div>
</div>
  </div>
</section>

<script>
if (typeof window !== "undefined") {

  const images = [
    "/img-content1.jpg",
    "/img-content2.jpg",
    "/img-content3.jpg"
  ];

  let index = 0;
  const heroImage = document.getElementById("hero-slider");

  setInterval(() => {
    index = (index + 1) % images.length;

    heroImage.classList.remove("fade");

    setTimeout(() => {
      heroImage.src = images[index];
      heroImage.classList.add("fade");
    }, 200);

  }, 5000); // cambia cada 5 segundos
}
</script>

<style>
  /* ===== HERO ===== */
  .hero {
    padding: 5rem 3rem;
    background: linear-gradient(to right, #f4f4f4, #c9d9e4);
  }

  .hero-container {
    max-width: 1200px;
    margin: auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 4rem;
  }

  /* ===== IZQUIERDA ===== */

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: #dbe8f2;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
    color: #0b4a8f;
  }

  .hero-badge .dot {
    width: 8px;
    height: 8px;
    background: #0b4a8f;
    border-radius: 50%;
  }

  .hero-content h1 {
    font-size: 3.5rem;
    line-height: 1.1;
    font-weight: 800;
    color: #0f172a;
  }

  .hero-content .blue {
    display: block;
    color: #0b6ea8;
  }

  .hero-content p {
    margin-top: 1rem;
    font-size: 1rem;
    color: #4b5563;
    max-width: 500px;
  }

  /* ===== BOTONES ===== */

  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    margin-top: 2rem;
  }

  .btn-primary {
    background: #0b4a8f;
    color: white;
    padding: 0.9rem 1.8rem;
    border-radius: 30px;
    text-decoration: none;
    font-weight: 600;
    transition: 0.3s;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
  }

  .btn-primary:hover {
    background: #083b72;
    transform: translateY(-2px);
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.9rem 1.8rem;
    border-radius: 30px;
    border: 1px solid #cbd5e1;
    text-decoration: none;
    color: #0f172a;
    font-weight: 500;
    background: white;
    transition: 0.3s;
  }

  .btn-secondary:hover {
    background: #f1f5f9;
  }

  /* ===== IMAGEN ===== */

.hero-image {
  display: flex;
  justify-content: center;
}

.image-card {
  padding: 1rem;
  border-radius: 30px;
  border: 3px solid #0b6ea8;
  background: rgba(255,255,255,0.4);
  backdrop-filter: blur(10px);
  box-shadow: 0 25px 50px rgba(0,0,0,0.15);
   transition: transform 0.4s ease;
}


.image-card:hover {
  transform: translateY(-6px);
}

.image-card img {
  width: 100%;
  max-width: 420px;
  border-radius: 18px;
  object-fit: cover;
  opacity: 1;
  transition: opacity 0.6s ease;
}

.image-card img:not(.fade) {
  opacity: 0.2;
}

  /* ===== RESPONSIVE ===== */

  @media (max-width: 992px) {
    .hero-container {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-content p {
      margin-left: auto;
      margin-right: auto;
    }

    .hero-buttons {
      justify-content: center;
    }
  }
</style>
