<section class="features">
  <div class="features-container">

    <div class="feature-card">
      <div class="icon-wrapper">
        <img src="/public/icons/icon_reloj.png" alt="Siempre al día" />
      </div>
      <h3>Siempre al día</h3>
      <p>Actualizado con la DIAN y evita multas.</p>
    </div>

    <div class="feature-card">
      <div class="icon-wrapper">
        <img src="/public/icons/icon_tiempo.png" alt="Ahorra tiempo" />
      </div>
      <h3>Ahorra tiempo</h3>
      <p>Simplifica tus labores contables diarias.</p>
    </div>

    <div class="feature-card">
      <div class="icon-wrapper">
        <img src="/public/icons/icon_check.png" alt="Sin errores" />
      </div>
      <h3>Sin errores</h3>
      <p>Automatiza tus tareas y reduce riesgos.</p>
    </div>

    <div class="feature-card">
      <div class="icon-wrapper">
        <img src="/public/icons/icon_tiket.png" alt="Saldos iniciales" />
      </div>
      <h3>Saldos iniciales</h3>
      <p>Registro de manera sencilla y rápida.</p>
    </div>

    <div class="feature-card">
      <div class="icon-wrapper">
        <img src="/public/icons/icon_clientes.png" alt="Gestiona clientes" />
      </div>
      <h3>Gestiona clientes</h3>
      <p>Centraliza todo en una única plataforma.</p>
    </div>

  </div>
</section>

<style>
  /* ===== FEATURES SECTION ===== */

.features {
  padding: 4rem 3rem;
  background: #f5f6f8;
}

.features-container {
  max-width: 1200px;
  margin: auto;
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 2rem;
}

/* ===== CARD ===== */

.feature-card {
  background: #ffffff;
  padding: 2rem 1.5rem;
  border-radius: 20px;
  text-align: left;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.04);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
  border: 1px solid #e5e7eb;
}

.feature-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
}

/* ===== ICONO ===== */

.icon-wrapper {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  background: #e6edf5;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 1.2rem;
}

.icon-wrapper img {
  width: 24px;
  height: 24px;
  object-fit: contain;
}

/* ===== TEXTOS ===== */

.feature-card h3 {
  font-size: 1.05rem;
  font-weight: 700;
  color: #0f172a;
  margin-bottom: 0.6rem;
}

.feature-card p {
  font-size: 0.9rem;
  color: #64748b;
  line-height: 1.4;
}

/* ===== RESPONSIVE ===== */

/* Tablet */
@media (max-width: 1100px) {
  .features-container {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* Móvil grande */
@media (max-width: 768px) {
  .features-container {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Móvil pequeño */
@media (max-width: 480px) {
  .features-container {
    grid-template-columns: 1fr;
  }

  .feature-card {
    text-align: center;
  }

  .icon-wrapper {
    margin-left: auto;
    margin-right: auto;
  }
}
</style>